var discrete = require('../lib/discrete'),
    expect = require('chai').expect

describe('discrete', function() {

    it('should be ok', function() {
        return expect(discrete).to.be.ok
    })

})
